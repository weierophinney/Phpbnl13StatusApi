<?php
// JSON representation of collection
$collection = array(
    'count'    => 'Total number of items in collection',
    'page'     => 'Current page being represented',
    'per_page' => 'Number of status entries returned per page',
);
echo json_encode($collection);

